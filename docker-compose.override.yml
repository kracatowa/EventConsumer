services:
  eventconsumer:
    image: ${DOCKER_REGISTRY-}eventconsumer:dev
    environment:
      - ASPNETCORE_ENVIRONMENT=LocalDocker
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/home/app/.microsoft/usersecrets:ro
      - ${APPDATA}/ASP.NET/Https:/home/app/.aspnet/https:ro